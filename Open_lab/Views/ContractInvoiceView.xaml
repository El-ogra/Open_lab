<UserControl x:Class="Open_lab.Views.ContractInvoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <GroupBox Header="اختيار الجهة والفترة" Grid.Row="0" Margin="0,0,0,8">
            <StackPanel Margin="8">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="جهة التعاقد:" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <ComboBox ItemsSource="{Binding Referrals}" 
                              SelectedValue="{Binding SelectedReferralId}" 
                              SelectedValuePath="ReferralId"
                              DisplayMemberPath="Name" 
                              Width="250" Margin="0,0,12,0" />
                    <Button Content="تحميل الفواتير المعلقة" Command="{Binding LoadPendingCommand}" Margin="0,0,8,0" />
                    <Button Content="عرض التاريخ" Command="{Binding LoadHistoryCommand}" />
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="من:" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <DatePicker SelectedDate="{Binding From}" Width="140" />
                    <TextBlock Text="إلى:" VerticalAlignment="Center" Margin="12,0,8,0" />
                    <DatePicker SelectedDate="{Binding To}" Width="140" />
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="فواتير معلقة للتجميع" Grid.Row="1" Margin="0,0,0,8">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <DataGrid ItemsSource="{Binding PendingInvoices}" AutoGenerateColumns="False" MaxHeight="200">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="رقم الفاتورة" Binding="{Binding InvoiceId}" Width="90" />
                        <DataGridTextColumn Header="Lab ID" Binding="{Binding LabId}" Width="100" />
                        <DataGridTextColumn Header="المريض" Binding="{Binding PatientName}" Width="150" />
                        <DataGridTextColumn Header="تاريخ الزيارة" Binding="{Binding VisitDate, StringFormat=yyyy-MM-dd}" Width="120" />
                        <DataGridTextColumn Header="الإجمالي" Binding="{Binding Total, StringFormat=N2}" Width="100" />
                        <DataGridTextColumn Header="الخصم" Binding="{Binding Discount, StringFormat=N2}" Width="100" />
                        <DataGridTextColumn Header="الصافي" Binding="{Binding NetTotal, StringFormat=N2}" Width="100" />
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,0">
                    <TextBlock Text="رقم فاتورة التعاقد:" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <TextBox Text="{Binding InvoiceNumber}" Width="150" Margin="0,0,12,0" />
                    <TextBlock Text="إجمالي المعلق:" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="Bold" />
                    <TextBlock Text="{Binding TotalPending, StringFormat=N2}" VerticalAlignment="Center" Margin="0,0,24,0" FontWeight="Bold" />
                    <Button Content="إنشاء فاتورة تعاقد" Command="{Binding CreateInvoiceCommand}" />
                </StackPanel>
            </Grid>
        </GroupBox>

        <GroupBox Header="تاريخ فواتير التعاقد" Grid.Row="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <DataGrid ItemsSource="{Binding ContractInvoices}" AutoGenerateColumns="False" SelectedItem="{Binding SelectedContractInvoice}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="رقم الفاتورة" Binding="{Binding ContractInvoiceId}" Width="90" />
                        <DataGridTextColumn Header="رقم التعاقد" Binding="{Binding InvoiceNumber}" Width="150" />
                        <DataGridTextColumn Header="من" Binding="{Binding DateFrom, StringFormat=yyyy-MM-dd}" Width="100" />
                        <DataGridTextColumn Header="إلى" Binding="{Binding DateTo, StringFormat=yyyy-MM-dd}" Width="100" />
                        <DataGridTextColumn Header="الإجمالي" Binding="{Binding TotalAmount, StringFormat=N2}" Width="110" />
                        <DataGridTextColumn Header="الخصم" Binding="{Binding DiscountAmount, StringFormat=N2}" Width="100" />
                        <DataGridTextColumn Header="الصافي" Binding="{Binding NetAmount, StringFormat=N2}" Width="110" />
                        <DataGridCheckBoxColumn Header="مدفوعة" Binding="{Binding IsPaid}" Width="80" />
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,0">
                    <Button Content="تسوية الفاتورة المحددة" Command="{Binding SettleSelectedCommand}" />
                </StackPanel>
            </Grid>
        </GroupBox>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="DarkGreen" TextWrapping="Wrap" Margin="0,8,0,0" />
    </Grid>
</UserControl>
