<UserControl x:Class="Open_lab.Views.SystemUsageMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <WrapPanel Margin="0,0,0,12">
            <Button Content="تحديث" Command="{Binding RefreshCommand}" />
        </WrapPanel>

        <TextBlock Grid.Row="1" Text="{Binding StatusMessage}" Foreground="DarkGreen" Margin="0,0,0,8" />

        <DataGrid Grid.Row="2" ItemsSource="{Binding Sessions}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="المستخدم" Binding="{Binding Username}" Width="150" />
                <DataGridTextColumn Header="الاسم الكامل" Binding="{Binding FullName}" Width="200" />
                <DataGridTextColumn Header="وقت الدخول" Binding="{Binding LoginAt, StringFormat=yyyy-MM-dd HH:mm}" Width="170" />
                <DataGridTextColumn Header="آخر نشاط" Binding="{Binding LastActivityAt, StringFormat=yyyy-MM-dd HH:mm}" Width="170" />
                <DataGridTextColumn Header="الحالة" Binding="{Binding Status}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
