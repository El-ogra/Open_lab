<UserControl x:Class="Open_lab.Views.Statistics.StatisticsModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft">
    <UserControl.Resources>
        <Style x:Key="ModuleButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="250" />
            <Setter Property="Height" Value="70" />
            <Setter Property="Margin" Value="10" />
            <Setter Property="Background" Value="#8DB600" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="12">
                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.88" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.75" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#25333E">
        <Grid.RowDefinitions>
            <RowDefinition Height="215" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Margin="20"
                Padding="22"
                Background="#FAFAFA"
                BorderBrush="#D5D8DA"
                BorderThickness="1"
                CornerRadius="3">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="260" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0" FlowDirection="LeftToRight">
                    <Ellipse Width="126" Height="126" Fill="#E7F2F4" Stroke="#B5CDD2" StrokeThickness="2" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    <TextBlock Text="&#xE9D2;" FontFamily="Segoe MDL2 Assets" Foreground="#376C78" FontSize="64" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Grid>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="من خلال هذه النافذة يمكنك عمل الآتي:"
                               Foreground="#D56E13"
                               FontSize="20"
                               FontWeight="Bold"
                               Margin="0,0,0,12" />
                    <TextBlock Text=". عمل احصاليات لعدد المرضى في فترات زمنية وتصنيفهم حسب الجنس والإحالة وطريقة الحساب" FontSize="16" Foreground="#111" Margin="0,3" />
                    <TextBlock Text=". عمل احصاليات لعدد عينات التحاليل وتصنيفهم حسب التحليل أو المجموعة أو جهة الإحالة" FontSize="16" Foreground="#111" Margin="0,3" />
                    <TextBlock Text=". عمل احصاليات للعينات المرسلة للخارج حسب الفترات الزمنية والمختبرات المرسل إليها العينات" FontSize="16" Foreground="#111" Margin="0,3" />
                    <TextBlock Text=". عمل احصاليات لمعرفة الطاقة الإنتاجية للمستخدمين حسب الأعمال الموكلة إليهم" FontSize="16" Foreground="#111" Margin="0,3" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Row="0" Grid.Column="0" Style="{StaticResource ModuleButtonStyle}" Command="{Binding OpenPatientCountStatCommand}">
                <DockPanel LastChildFill="True">
                    <TextBlock Text="&#xE9D2;" FontFamily="Segoe MDL2 Assets" FontSize="28" DockPanel.Dock="Left" Margin="0,0,12,0" />
                    <TextBlock Text="احصاليات وفقاً لعدد المرضى" TextWrapping="Wrap" TextAlignment="Right" VerticalAlignment="Center" />
                </DockPanel>
            </Button>
            <Button Grid.Row="0" Grid.Column="1" Style="{StaticResource ModuleButtonStyle}" Command="{Binding OpenTestCountStatCommand}">
                <DockPanel LastChildFill="True">
                    <TextBlock Text="&#xE9D2;" FontFamily="Segoe MDL2 Assets" FontSize="28" DockPanel.Dock="Left" Margin="0,0,12,0" />
                    <TextBlock Text="احصاليات وفقاً لعدد التحاليل" TextWrapping="Wrap" TextAlignment="Right" VerticalAlignment="Center" />
                </DockPanel>
            </Button>
            <Button Grid.Row="0" Grid.Column="2" Style="{StaticResource ModuleButtonStyle}" Command="{Binding OpenBranchStatCommand}">
                <DockPanel LastChildFill="True">
                    <TextBlock Text="&#xE9D2;" FontFamily="Segoe MDL2 Assets" FontSize="28" DockPanel.Dock="Left" Margin="0,0,12,0" />
                    <TextBlock Text="احصاليات خاصة بفروع المعمل" TextWrapping="Wrap" TextAlignment="Right" VerticalAlignment="Center" />
                </DockPanel>
            </Button>
            
            <Button Grid.Row="1" Grid.Column="0" Style="{StaticResource ModuleButtonStyle}" Command="{Binding OpenExternalSamplesStatCommand}">
                <DockPanel LastChildFill="True">
                    <TextBlock Text="&#xE9D2;" FontFamily="Segoe MDL2 Assets" FontSize="28" DockPanel.Dock="Left" Margin="0,0,12,0" />
                    <TextBlock Text="احصاليات العينات المرسلة" TextWrapping="Wrap" TextAlignment="Right" VerticalAlignment="Center" />
                </DockPanel>
            </Button>
            <Button Grid.Row="1" Grid.Column="1" Style="{StaticResource ModuleButtonStyle}" Command="{Binding OpenWorkPerformanceStatCommand}">
                <DockPanel LastChildFill="True">
                    <TextBlock Text="&#xE9D2;" FontFamily="Segoe MDL2 Assets" FontSize="28" DockPanel.Dock="Left" Margin="0,0,12,0" />
                    <TextBlock Text="احصاليات تقيم ومتابعة العمل" TextWrapping="Wrap" TextAlignment="Right" VerticalAlignment="Center" />
                </DockPanel>
            </Button>
            <Button Grid.Row="1" Grid.Column="2" Style="{StaticResource ModuleButtonStyle}" Command="{Binding OpenResultsMonitorCommand}">
                <DockPanel LastChildFill="True">
                    <TextBlock Text="&#xE9D2;" FontFamily="Segoe MDL2 Assets" FontSize="28" DockPanel.Dock="Left" Margin="0,0,12,0" />
                    <TextBlock Text="متابعة ومراقبة النتائج" TextWrapping="Wrap" TextAlignment="Right" VerticalAlignment="Center" />
                </DockPanel>
            </Button>
        </Grid>
    </Grid>
</UserControl>
