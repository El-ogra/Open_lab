<UserControl x:Class="Open_lab.Views.Patients.PatientRegistrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft"
             Background="#F4F7F2">
    <UserControl.Resources>
        <Style x:Key="PanelStyle" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#D7E0D2" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="Margin" Value="6" />
        </Style>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="95" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Margin" Value="4" />
            <Setter Property="Background" Value="#6E9F16" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,3" />
            <Setter Property="Foreground" Value="#263327" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.05*" />
            <ColumnDefinition Width="1.45*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource PanelStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="170" />
                </Grid.RowDefinitions>

                <DockPanel Margin="0,0,0,8">
                    <ComboBox Width="150" DockPanel.Dock="Right" SelectedIndex="0">
                        <ComboBoxItem Content="Routine Tests" />
                        <ComboBoxItem Content="Send Out" />
                        <ComboBoxItem Content="All" />
                    </ComboBox>
                    <TextBox Margin="8,0,0,0"
                             ToolTip="اكتب جزءاً من اسم التحليل للتصفية عند اكتمال دعم الفلترة."
                             Text="" />
                </DockPanel>

                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding AvailableTests}"
                          SelectedItem="{Binding SelectedAvailableTest}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="الكود" Binding="{Binding Code}" Width="80" />
                        <DataGridTextColumn Header="التحليل" Binding="{Binding NameReport}" Width="*" />
                        <DataGridTextColumn Header="السعر" Binding="{Binding Price}" Width="90" />
                    </DataGrid.Columns>
                </DataGrid>

                <UniformGrid Grid.Row="2" Columns="3" Margin="0,8">
                    <Button Content="Add selected test" Command="{Binding AddTestCommand}" Style="{StaticResource ActionButtonStyle}" />
                    <Button Content="Delete" Command="{Binding RemoveTestCommand}" Style="{StaticResource ActionButtonStyle}" Background="#A94442" />
                    <Button Content="All" Command="{Binding AddAllTestsCommand}" Style="{StaticResource ActionButtonStyle}" />
                </UniformGrid>

                <GroupBox Grid.Row="3" Header="Selected Test">
                    <DataGrid ItemsSource="{Binding SelectedTests}"
                              SelectedItem="{Binding SelectedTest}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="التحليل" Binding="{Binding TestName}" Width="*" />
                            <DataGridTextColumn Header="السعر" Binding="{Binding Price}" Width="90" />
                        </DataGrid.Columns>
                    </DataGrid>
                </GroupBox>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="0" Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Border Style="{StaticResource PanelStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="110" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="105" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Lab ID" Style="{StaticResource LabelStyle}" />
                        <DockPanel Grid.Row="0" Grid.Column="1">
                            <Button Content="توليد" Command="{Binding GenerateLabIdCommand}" Style="{StaticResource ActionButtonStyle}" MinWidth="58" DockPanel.Dock="Left" />
                            <TextBox Text="{Binding LabId, UpdateSourceTrigger=PropertyChanged}" />
                        </DockPanel>
                        <TextBlock Grid.Row="0" Grid.Column="2" Text="نظام الحساب" Style="{StaticResource LabelStyle}" />
                        <ComboBox Grid.Row="0" Grid.Column="3" Text="{Binding AccountType}" IsEditable="True">
                            <ComboBoxItem Content="Individual" />
                            <ComboBoxItem Content="Lab to Lab" />
                        </ComboBox>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="الجنس" Style="{StaticResource LabelStyle}" />
                        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal">
                            <RadioButton Content="ذكر" Margin="0,0,12,0" IsChecked="{Binding Gender, Converter={StaticResource StringEqualsConverter}, ConverterParameter=Male}" />
                            <RadioButton Content="أنثى" Margin="0,0,12,0" IsChecked="{Binding Gender, Converter={StaticResource StringEqualsConverter}, ConverterParameter=Female}" />
                            <RadioButton Content="غير محدد" IsChecked="{Binding Gender, Converter={StaticResource StringEqualsConverter}, ConverterParameter=Unknown}" />
                        </StackPanel>
                        <CheckBox Grid.Row="1" Grid.Column="2" Content="VIP" IsChecked="{Binding IsVip}" VerticalAlignment="Center" />
                        <ComboBox Grid.Row="1" Grid.Column="3" SelectedIndex="0">
                            <ComboBoxItem Content="السيد" />
                            <ComboBoxItem Content="السيدة" />
                            <ComboBoxItem Content="الآنسة" />
                            <ComboBoxItem Content="الطفل" />
                        </ComboBox>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="اسم المريض" Style="{StaticResource LabelStyle}" />
                        <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3"
                                 Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="يفضل كتابة الاسم بدون همزة، واستبدال التاء المربوطة بهاء عند البحث." />

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="السن" Style="{StaticResource LabelStyle}" />
                        <DockPanel Grid.Row="3" Grid.Column="1">
                            <ComboBox Width="90" DockPanel.Dock="Left" Text="{Binding AgeUnit}" IsEditable="True">
                                <ComboBoxItem Content="سنوات" />
                                <ComboBoxItem Content="أشهر" />
                                <ComboBoxItem Content="أيام" />
                            </ComboBox>
                            <TextBox Text="{Binding Age, UpdateSourceTrigger=PropertyChanged}" />
                        </DockPanel>
                        <TextBlock Grid.Row="3" Grid.Column="2" Text="تاريخ الميلاد" Style="{StaticResource LabelStyle}" />
                        <DatePicker Grid.Row="3" Grid.Column="3" SelectedDate="{Binding BirthDate}" />

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="العنوان" Style="{StaticResource LabelStyle}" />
                        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Grid.Row="4" Grid.Column="2" Text="المحمول" Style="{StaticResource LabelStyle}" />
                        <TextBox Grid.Row="4" Grid.Column="3" Text="{Binding MobilePhone, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="5" Grid.Column="0" Text="رقم قومي" Style="{StaticResource LabelStyle}" />
                        <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding NationalId, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Grid.Row="5" Grid.Column="2" Text="هاتف/بريد" Style="{StaticResource LabelStyle}" />
                        <StackPanel Grid.Row="5" Grid.Column="3" Orientation="Horizontal">
                            <TextBox Width="120" Text="{Binding HomePhone, UpdateSourceTrigger=PropertyChanged}" ToolTip="هاتف المنزل" />
                            <TextBox Width="180" Margin="6,0,0,0" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" ToolTip="البريد الإلكتروني" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Style="{StaticResource PanelStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="110" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="105" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="جهة الإحالة" Style="{StaticResource LabelStyle}" />
                        <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding Referrals}" SelectedItem="{Binding SelectedReferral}" DisplayMemberPath="Name" />
                        <CheckBox Grid.Row="0" Grid.Column="2" Content="طباعة" VerticalAlignment="Center" />
                        <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding SelectedReferral.Phone}" IsReadOnly="True" ToolTip="هاتف الجهة" />
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="عنوان الجهة" Style="{StaticResource LabelStyle}" />
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SelectedReferral.City}" IsReadOnly="True" />
                        <TextBlock Grid.Row="1" Grid.Column="2" Text="المسؤول" Style="{StaticResource LabelStyle}" />
                        <TextBox Grid.Row="1" Grid.Column="3" />
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Flags" Style="{StaticResource LabelStyle}" />
                        <WrapPanel Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3">
                            <CheckBox Content="صائم" IsChecked="{Binding IsFasting}" Margin="6" />
                            <TextBox Width="55" Text="{Binding FastingHours}" ToolTip="عدد ساعات الصيام" />
                            <CheckBox Content="مضاد حيوي" Margin="6" />
                            <CheckBox Content="علاج للسكر" Margin="6" />
                            <CheckBox Content="نقل دم" Margin="6" />
                            <CheckBox Content="علاج للغدد" Margin="6" />
                            <CheckBox Content="علاج للكبد" Margin="6" />
                            <CheckBox Content="حمل" Margin="6" />
                            <CheckBox Content="مدخن" Margin="6" />
                            <CheckBox Content="أشعة بالصبغة" Margin="6" />
                        </WrapPanel>
                    </Grid>
                </Border>

                <Border Style="{StaticResource PanelStyle}">
                    <StackPanel>
                        <WrapPanel Margin="0,0,0,8">
                            <ToggleButton Content="Blood" Margin="4" />
                            <ToggleButton Content="Urine" Margin="4" />
                            <ToggleButton Content="Stool" Margin="4" />
                            <ToggleButton Content="Semen" Margin="4" />
                            <ToggleButton Content="CSF" Margin="4" />
                            <ToggleButton Content="Taken outside Lab" Margin="4" />
                        </WrapPanel>
                        <TextBox MinHeight="70" Text="{Binding MedicalNotes, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource PanelStyle}">
            <UniformGrid Columns="4">
                <TextBlock Text="{Binding TotalAmount, StringFormat=إجمالي التحاليل: {0:N2}}" />
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="الخصم % " />
                    <TextBox Width="55" Text="{Binding DiscountPercent, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
                <TextBlock Text="{Binding DiscountAmount, StringFormat=قيمة الخصم: {0:N2}}" />
                <TextBlock Text="{Binding NetTotal, StringFormat=بعد الخصم: {0:N2}}" />
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="المدفوع " />
                    <TextBox Width="75" Text="{Binding PaidAmount, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
                <TextBlock Text="{Binding PreviousPaidAmount, StringFormat=المدفوع سابقا: {0:N2}}" />
                <TextBlock Text="{Binding BalanceForLab, StringFormat=الباقي للمعمل: {0:N2}}" />
                <TextBlock Text="{Binding BalanceForPatient, StringFormat=الباقي للمريض: {0:N2}}" />
            </UniformGrid>
        </Border>

        <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource PanelStyle}">
            <WrapPanel HorizontalAlignment="Center">
                <Button Content="اضافة مريض" Command="{Binding NewCommand}" Style="{StaticResource ActionButtonStyle}" />
                <Button Content="ضبط" Command="{Binding SaveCommand}" Style="{StaticResource ActionButtonStyle}" />
                <Button Content="تحميل Lab ID" Command="{Binding LoadByLabIdCommand}" Style="{StaticResource ActionButtonStyle}" />
                <Button Content="بحث" Command="{Binding SearchCommand}" Style="{StaticResource ActionButtonStyle}" />
                <Button Content="حذف" Command="{Binding DeleteCommand}" Style="{StaticResource ActionButtonStyle}" Background="#A94442" />
                <Button Content="الباركود" Command="{Binding ShowBarcodeCommand}" Style="{StaticResource ActionButtonStyle}" />
                <Button Content="طباعة إيصال" Command="{Binding PrintReceiptCommand}" Style="{StaticResource ActionButtonStyle}" />
                <Button Content="نتائج التحاليل" Command="{Binding DataContext.NavigateResultsEntryCommand, RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource ActionButtonStyle}" />
                <Button Content="القائمة الرئيسية" Command="{Binding DataContext.NavigateToPatientsCommand, RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource ActionButtonStyle}" />
            </WrapPanel>
        </Border>

        <TextBlock Grid.Row="2" Grid.ColumnSpan="2" Text="{Binding StatusMessage}" Foreground="#245423" Margin="10,2" />
    </Grid>
</UserControl>
