<UserControl x:Class="Open_lab.Views.Patients.DeliveryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft"
             Background="#F3F7F0">
    <UserControl.InputBindings>
        <KeyBinding Key="F5" Command="{Binding SearchCommand}" />
        <KeyBinding Key="Enter" Command="{Binding DeliverCommand}" />
    </UserControl.InputBindings>
    <UserControl.Resources>
        <Style x:Key="PanelStyle" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#D7E0D2" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="Margin" Value="6" />
        </Style>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="95" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Margin" Value="4" />
            <Setter Property="Background" Value="#6E9F16" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="390" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Grid.ColumnSpan="2" Style="{StaticResource PanelStyle}">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <TextBlock Text="من" VerticalAlignment="Center" Margin="0,0,6,0" />
                    <DatePicker SelectedDate="{Binding DateFrom}" Width="130" />
                    <TextBlock Text="إلى" VerticalAlignment="Center" Margin="12,0,6,0" />
                    <DatePicker SelectedDate="{Binding DateTo}" Width="130" />
                    <TextBox Width="210" Margin="10,0" Text="{Binding Keyword, UpdateSourceTrigger=PropertyChanged}" ToolTip="بحث سريع بكود الحالة أو المعمل أو الملف" />
                    <Button Content="بحث / F5" Command="{Binding SearchCommand}" Style="{StaticResource ActionButtonStyle}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Content="F2 بيانات" Command="{Binding DataContext.NavigatePatientRegistrationCommand, RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource ActionButtonStyle}" />
                    <Button Content="F3 بحث" Command="{Binding DataContext.NavigatePatientSearchCommand, RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource ActionButtonStyle}" />
                    <Button Content="F4 نتائج" Command="{Binding DataContext.NavigateResultsEntryCommand, RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource ActionButtonStyle}" />
                    <Button Content="القائمة الرئيسية" Command="{Binding DataContext.NavigateToPatientsCommand, RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource ActionButtonStyle}" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource PanelStyle}">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="مرضى اليوم غير المسلمة أو نتائج البحث" FontSize="16" FontWeight="Bold" Margin="0,0,0,8" />
                <DataGrid ItemsSource="{Binding Visits}"
                          SelectedItem="{Binding SelectedVisit}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Lab ID" Binding="{Binding LabId}" Width="95" />
                        <DataGridTextColumn Header="المريض" Binding="{Binding PatientName}" Width="*" />
                        <DataGridTextColumn Header="جاهز" Binding="{Binding IsReadyForDelivery}" Width="65" />
                        <DataGridTextColumn Header="مسلم" Binding="{Binding IsDelivered}" Width="65" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="170" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource PanelStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="130" />
                        <ColumnDefinition Width="130" />
                        <ColumnDefinition Width="130" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding SelectedVisit.PatientName, TargetNullValue=لم يتم اختيار مريض}" FontSize="19" FontWeight="Bold" />
                    <TextBlock Grid.Column="1" Text="{Binding SelectedVisit.LabId, StringFormat=Lab ID: {0}}" />
                    <TextBlock Grid.Column="2" Text="{Binding SelectedVisit.VisitDate, StringFormat=تاريخ: {0:yyyy/MM/dd}}" />
                    <TextBlock Grid.Column="3" Text="{Binding SelectedVisit.VisitStatus, StringFormat=حالة: {0}}" />
                </Grid>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource PanelStyle}">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="تحاليل وحالة زيارة المريض المحدد" FontSize="16" FontWeight="Bold" Margin="0,0,0,8" />
                    <DataGrid ItemsSource="{Binding Visits}"
                              SelectedItem="{Binding SelectedVisit}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="الزيارة" Binding="{Binding VisitId}" Width="90" />
                            <DataGridTextColumn Header="عدد التحاليل" Binding="{Binding TestsCount}" Width="110" />
                            <DataGridTextColumn Header="تمت المراجعة" Binding="{Binding VerifiedCount}" Width="120" />
                            <DataGridTextColumn Header="تم التسليم" Binding="{Binding DeliveredCount}" Width="120" />
                            <DataGridTextColumn Header="الباقي" Binding="{Binding Balance}" Width="110" />
                            <DataGridTextColumn Header="جاهز" Binding="{Binding IsReadyForDelivery}" Width="90" />
                            <DataGridTextColumn Header="مسلم" Binding="{Binding IsDelivered}" Width="90" />
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>

            <Border Grid.Row="2" Style="{StaticResource PanelStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="260" />
                    </Grid.ColumnDefinitions>
                    <UniformGrid Columns="2">
                        <TextBlock Text="{Binding SelectedVisit.TestsCount, StringFormat=إجمالي التحاليل: {0}}" Margin="4" />
                        <TextBlock Text="{Binding SelectedVisit.VerifiedCount, StringFormat=المراجعة: {0}}" Margin="4" />
                        <TextBlock Text="{Binding SelectedVisit.DeliveredCount, StringFormat=المسلم: {0}}" Margin="4" />
                        <TextBlock Text="{Binding SelectedVisit.Balance, StringFormat=الباقي للمعمل: {0:N2}}" Margin="4" />
                        <TextBlock Text="الباقي للمريض: 0.00" Margin="4" />
                        <TextBlock Text="{Binding SelectedVisit.IsReadyForDelivery, StringFormat=جاهز للتسليم: {0}}" Margin="4" />
                    </UniformGrid>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <Button Content="تسليم / Enter" Command="{Binding DeliverCommand}" Style="{StaticResource ActionButtonStyle}" />
                        <Button Content="إعادة فتح التسليم" Command="{Binding ReopenCommand}" Style="{StaticResource ActionButtonStyle}" Background="#D98B20" />
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="دفع مبلغ" VerticalAlignment="Center" />
                            <TextBox Width="90" Margin="8,0" Text="{Binding PaymentAmount, UpdateSourceTrigger=PropertyChanged}" />
                            <Button Content="تسديد" Command="{Binding PayCommand}" Style="{StaticResource ActionButtonStyle}" />
                        </StackPanel>
                        <Button Content="مستلمة" Command="{Binding DeliverCommand}" Style="{StaticResource ActionButtonStyle}" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Grid.ColumnSpan="2" Style="{StaticResource PanelStyle}">
            <DockPanel>
                <WrapPanel DockPanel.Dock="Right">
                    <CheckBox Content="معمل لمعمل" Margin="8,0" />
                    <CheckBox Content="مريض مستقل" Margin="8,0" />
                    <CheckBox Content="أشخاص مهمين" Margin="8,0" />
                    <CheckBox Content="الكل" Margin="8,0" />
                </WrapPanel>
                <TextBlock Text="{Binding StatusMessage}" Foreground="#245423" VerticalAlignment="Center" />
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
