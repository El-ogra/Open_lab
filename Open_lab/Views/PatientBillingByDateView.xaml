<UserControl x:Class="Open_lab.Views.PatientBillingByDateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <WrapPanel Margin="0,0,0,12">
            <TextBlock Text="رقم المريض" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox Width="120" Text="{Binding PatientId, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="من" VerticalAlignment="Center" Margin="12,0,8,0" />
            <DatePicker Width="140" SelectedDate="{Binding FromDate}" />
            <TextBlock Text="إلى" VerticalAlignment="Center" Margin="12,0,8,0" />
            <DatePicker Width="140" SelectedDate="{Binding ToDate}" />
            <Button Content="بحث" Margin="12,0,0,0" Command="{Binding SearchCommand}" />
        </WrapPanel>

        <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,12">
            <Border Margin="4" Padding="8" Background="#F4F4F4">
                <StackPanel>
                    <TextBlock Text="إجمالي الفواتير" />
                    <TextBlock Text="{Binding TotalInvoiced, StringFormat=N2}" FontWeight="Bold" />
                </StackPanel>
            </Border>
            <Border Margin="4" Padding="8" Background="#F4F4F4">
                <StackPanel>
                    <TextBlock Text="إجمالي المدفوع" />
                    <TextBlock Text="{Binding TotalPaid, StringFormat=N2}" FontWeight="Bold" />
                </StackPanel>
            </Border>
            <Border Margin="4" Padding="8" Background="#F4F4F4">
                <StackPanel>
                    <TextBlock Text="الرصيد" />
                    <TextBlock Text="{Binding Balance, StringFormat=N2}" FontWeight="Bold" />
                </StackPanel>
            </Border>
        </UniformGrid>

        <GroupBox Grid.Row="2" Header="الفواتير">
            <DataGrid ItemsSource="{Binding Invoices}" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="رقم الفاتورة" Binding="{Binding InvoiceId}" Width="120" />
                    <DataGridTextColumn Header="التاريخ" Binding="{Binding Visit.VisitDate, StringFormat=yyyy-MM-dd}" Width="140" />
                    <DataGridTextColumn Header="الإجمالي" Binding="{Binding Total, StringFormat=N2}" Width="120" />
                    <DataGridTextColumn Header="الخصم" Binding="{Binding Discount, StringFormat=N2}" Width="120" />
                    <DataGridTextColumn Header="الصافي" Binding="{Binding NetTotal, StringFormat=N2}" Width="120" />
                    <DataGridTextColumn Header="المدفوع" Binding="{Binding Paid, StringFormat=N2}" Width="120" />
                    <DataGridTextColumn Header="المتبقي" Binding="{Binding Balance, StringFormat=N2}" Width="120" />
                    <DataGridTextColumn Header="الحالة" Binding="{Binding Status}" Width="110" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <GroupBox Grid.Row="3" Header="المدفوعات" Margin="0,8,0,0">
            <DataGrid ItemsSource="{Binding Payments}" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="رقم الدفعة" Binding="{Binding PaymentId}" Width="120" />
                    <DataGridTextColumn Header="التاريخ" Binding="{Binding PaymentDate, StringFormat=yyyy-MM-dd HH:mm}" Width="160" />
                    <DataGridTextColumn Header="المبلغ" Binding="{Binding Amount, StringFormat=N2}" Width="130" />
                    <DataGridTextColumn Header="المستخدم" Binding="{Binding UserId}" Width="110" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</UserControl>
