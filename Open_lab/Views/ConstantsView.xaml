<UserControl x:Class="Open_lab.Views.ConstantsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button Content="تحميل" Command="{Binding ReloadCommand}" />
            <Button Content="إدراج الافتراضي" Margin="8,0,0,0" Command="{Binding SeedDefaultsCommand}" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,10">
            <TextBlock Text="الثابت" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox Width="280" Text="{Binding Key, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="القيمة" VerticalAlignment="Center" Margin="12,0,8,0" />
            <TextBox Width="300" Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="حفظ" Margin="12,0,0,0" Command="{Binding SaveCommand}" />
            <Button Content="حذف" Margin="8,0,0,0" Command="{Binding DeleteCommand}" />
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Items}" SelectedItem="{Binding Selected}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Key" Binding="{Binding Key}" Width="300" />
                <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="DarkGreen" Margin="0,10,0,0" />
    </Grid>
</UserControl>
