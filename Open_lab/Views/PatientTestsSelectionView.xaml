<UserControl x:Class="Open_lab.Views.PatientTestsSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <StackPanel Margin="0,0,16,0">
            <TextBlock Text="اختيار التحاليل" FontSize="20" FontWeight="Bold" />
            <TextBlock Text="Lab ID" Margin="0,12,0,4" />
            <TextBox Text="{Binding LabId, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="تحميل المريض" Margin="0,8,0,0" Command="{Binding LoadPatientCommand}" />

            <TextBlock Text="اسم المريض" Margin="0,12,0,4" />
            <TextBox Text="{Binding PatientName}" IsReadOnly="True" />

            <TextBlock Text="نوع الحساب" Margin="0,12,0,4" />
            <ComboBox ItemsSource="{Binding AccountTypes}" SelectedItem="{Binding SelectedAccountType}" />

            <TextBlock Text="الجهة (للتحويل)" Margin="0,12,0,4" />
            <ComboBox ItemsSource="{Binding Referrals}" SelectedItem="{Binding SelectedReferral}" DisplayMemberPath="Name" />

            <Button Content="إنشاء زيارة جديدة" Margin="0,12,0,0" Command="{Binding CreateVisitCommand}" />
            <TextBlock Text="رقم الزيارة" Margin="0,12,0,4" />
            <TextBox Text="{Binding VisitId}" IsReadOnly="True" />

            <TextBlock Text="إجمالي التحاليل" Margin="0,12,0,4" />
            <TextBox Text="{Binding TotalAmount, StringFormat=N2}" IsReadOnly="True" />

            <TextBlock Text="{Binding StatusMessage}" Foreground="DarkGreen" Margin="0,12,0,0" TextWrapping="Wrap" />
        </StackPanel>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <GroupBox Header="البحث وإضافة التحاليل">
                <StackPanel>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />
                    <ComboBox ItemsSource="{Binding FilteredAvailableTests}" SelectedItem="{Binding SelectedAvailableTest}" DisplayMemberPath="NameReport" />
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Button Content="إضافة تحليل" Command="{Binding AddTestCommand}" Margin="0,0,8,0" />
                        <Button Content="تحديث القائمة" Command="{Binding RefreshTestsCommand}" />
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <GroupBox Grid.Row="1" Header="المجموعات الجاهزة" Margin="0,12,0,0">
                <StackPanel>
                    <ComboBox ItemsSource="{Binding CustomGroups}" SelectedItem="{Binding SelectedCustomGroup}" DisplayMemberPath="Name" />
                    <Button Content="إضافة المجموعة للزيارة" Margin="0,8,0,0" Command="{Binding AddCustomGroupCommand}" />
                </StackPanel>
            </GroupBox>

            <GroupBox Grid.Row="2" Header="تحاليل الزيارة" Margin="0,12,0,0">
                <StackPanel>
                    <DataGrid ItemsSource="{Binding SelectedTests}" SelectedItem="{Binding SelectedVisitTest}" AutoGenerateColumns="False" Height="260">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="التحليل" Binding="{Binding TestName}" Width="*" />
                            <DataGridTextColumn Header="السعر" Binding="{Binding Price, StringFormat=N2}" Width="120" />
                        </DataGrid.Columns>
                    </DataGrid>
                    <Button Content="حذف التحليل المحدد" Margin="0,8,0,0" Command="{Binding RemoveTestCommand}" />
                </StackPanel>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>
