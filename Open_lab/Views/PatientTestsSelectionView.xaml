<UserControl x:Class="Open_lab.Views.PatientTestsSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <StackPanel Margin="0,0,16,0">
            <TextBlock Text="اختيار التحاليل" FontSize="20" FontWeight="Bold" />
            <TextBlock Text="Lab ID" Margin="0,12,0,4" />
            <TextBox Text="{Binding LabId, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="تحميل المريض" Margin="0,8,0,0" Command="{Binding LoadPatientCommand}" />

            <TextBlock Text="اسم المريض" Margin="0,12,0,4" />
            <TextBox Text="{Binding PatientName}" IsReadOnly="True" />

            <TextBlock Text="نوع الحساب" Margin="0,12,0,4" />
            <ComboBox ItemsSource="{Binding AccountTypes}" SelectedItem="{Binding SelectedAccountType}" />

            <TextBlock Text="الجهة (للتحويل)" Margin="0,12,0,4" />
            <ComboBox ItemsSource="{Binding Referrals}" SelectedItem="{Binding SelectedReferral}" DisplayMemberPath="Name" />

            <Button Content="إنشاء زيارة جديدة" Margin="0,12,0,0" Command="{Binding CreateVisitCommand}" />
            <TextBlock Text="رقم الزيارة" Margin="0,12,0,4" />
            <TextBox Text="{Binding VisitId}" IsReadOnly="True" />

            <TextBlock Text="{Binding StatusMessage}" Foreground="DarkGreen" Margin="0,12,0,0" />
        </StackPanel>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock Text="التحاليل المتاحة" FontWeight="Bold" />
                <ComboBox ItemsSource="{Binding AvailableTests}" SelectedItem="{Binding SelectedAvailableTest}" DisplayMemberPath="NameReport" />
                <Button Content="إضافة تحليل" Margin="0,8,0,0" Command="{Binding AddTestCommand}" />
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="0,12,0,0">
                <TextBlock Text="تحاليل الزيارة" FontWeight="Bold" />
                <DataGrid ItemsSource="{Binding SelectedTests}" SelectedItem="{Binding SelectedVisitTest}" AutoGenerateColumns="False" Height="200">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="التحليل" Binding="{Binding TestName}" Width="*" />
                        <DataGridTextColumn Header="السعر" Binding="{Binding Price}" Width="100" />
                    </DataGrid.Columns>
                </DataGrid>
                <Button Content="حذف" Margin="0,8,0,0" Command="{Binding RemoveTestCommand}" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
