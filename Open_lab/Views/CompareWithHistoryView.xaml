<UserControl x:Class="Open_lab.Views.CompareWithHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <GroupBox Header="بيانات المريض" Grid.Row="0" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal" Margin="8">
                <TextBlock Text="Lab ID:" VerticalAlignment="Center" Margin="0,0,8,0" />
                <TextBox Text="{Binding LabId}" Width="120" Margin="0,0,12,0" />
                <Button Content="تحميل المريض" Command="{Binding LoadPatientCommand}" Margin="0,0,8,0" />
                <Button Content="مسح" Command="{Binding ClearCommand}" />
            </StackPanel>
        </GroupBox>

        <GroupBox Header="اختيار التحليل" Grid.Row="1" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal" Margin="8">
                <TextBlock Text="التحليل:" VerticalAlignment="Center" Margin="0,0,8,0" />
                <ComboBox ItemsSource="{Binding Tests}" 
                          SelectedValue="{Binding SelectedTestId}" 
                          SelectedValuePath="TestId"
                          DisplayMemberPath="TestName" 
                          Width="300" Margin="0,0,12,0" />
                <TextBlock Text="عدد النتائج:" VerticalAlignment="Center" Margin="0,0,8,0" />
                <ComboBox SelectedItem="{Binding HistoryCount}" Width="80" Margin="0,0,12,0">
                    <ComboBoxItem Content="3" />
                    <ComboBoxItem Content="5" />
                    <ComboBoxItem Content="10" />
                </ComboBox>
                <Button Content="عرض التاريخ" Command="{Binding LoadHistoryCommand}" />
            </StackPanel>
        </GroupBox>

        <DataGrid Grid.Row="3" ItemsSource="{Binding HistoryResults}" AutoGenerateColumns="False" Margin="0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="تاريخ الزيارة" Binding="{Binding VisitDate, StringFormat=yyyy-MM-dd HH:mm}" Width="150" />
                <DataGridTextColumn Header="رقم الزيارة" Binding="{Binding VisitId}" Width="100" />
                <DataGridTextColumn Header="المعامل" Binding="{Binding ParameterName}" Width="200" />
                <DataGridTextColumn Header="القيمة" Binding="{Binding Value}" Width="120" />
                <DataGridTextColumn Header="الإشارة" Binding="{Binding Flag}" Width="80" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="4" Text="{Binding StatusMessage}" Foreground="DarkGreen" TextWrapping="Wrap" />
    </Grid>
</UserControl>
