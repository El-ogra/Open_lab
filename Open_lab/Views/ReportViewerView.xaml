<UserControl x:Class="Open_lab.Views.ReportViewerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <StackPanel>
            <TextBlock Text="عرض التقرير" FontSize="20" FontWeight="Bold" />
            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                <TextBlock Text="رقم الزيارة" VerticalAlignment="Center" Margin="0,0,8,0" />
                <TextBox Text="{Binding VisitId, UpdateSourceTrigger=PropertyChanged}" Width="120" />
                <Button Content="تحميل" Margin="12,0,0,0" Command="{Binding LoadReportCommand}" />
                <Button Content="طباعة" Margin="8,0,0,0" Command="{Binding PrintCommand}" />
                <Button Content="إعادة طباعة" Margin="8,0,0,0" Command="{Binding ReprintCommand}" />
            </StackPanel>

            <TextBlock Text="{Binding StatusMessage}" Foreground="DarkGreen" Margin="0,8,0,0" TextWrapping="Wrap" />

            <GroupBox Header="بيانات المريض" Margin="0,12,0,0">
                <StackPanel Margin="8">
                    <TextBlock Text="{Binding Report.Patient.FullName}" FontWeight="Bold" />
                    <TextBlock Text="{Binding Report.Visit.VisitDate}" />
                </StackPanel>
            </GroupBox>

            <GroupBox Header="التحاليل" Margin="0,12,0,0">
                <ItemsControl ItemsSource="{Binding Tests}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,4,0,4">
                                <TextBlock Text="{Binding Test.NameReport}" FontWeight="Bold" />
                                <ItemsControl ItemsSource="{Binding Results}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Parameter.Name}" Width="200" />
                                                <TextBlock Text="{Binding Value}" Width="120" />
                                                <TextBlock Text="{Binding Flag}" Width="60" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </GroupBox>

            <GroupBox Header="معاينة قبل الطباعة" Margin="0,12,0,0">
                <TextBox Text="{Binding PreviewContent}"
                         Height="300"
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto" />
            </GroupBox>
        </StackPanel>
    </Grid>
</UserControl>
