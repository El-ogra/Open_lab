<UserControl x:Class="Open_lab.Views.Shared.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft">
    <Grid Background="#F7FAF7">
        <Border Width="520"
                MinHeight="210"
                Padding="32"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="White"
                BorderBrush="#D7DFD8"
                BorderThickness="1"
                CornerRadius="5">
            <StackPanel>
                <TextBlock Text="{Binding FunctionTitle}"
                           FontSize="30"
                           FontWeight="Bold"
                           Foreground="#203B2B"
                           TextAlignment="Center"
                           Margin="0,0,0,18" />
                <TextBlock Text="نافذة مؤقتة للوظيفة، وسيتم استكمال محتواها لاحقاً."
                           FontSize="16"
                           Foreground="#3F4F46"
                           TextAlignment="Center"
                           Margin="0,0,0,28" />
                <Button Content="رجوع"
                        Command="{Binding BackCommand}"
                        Width="150"
                        Height="42"
                        HorizontalAlignment="Center"
                        Background="#234B67"
                        Foreground="White"
                        FontSize="16"
                        FontWeight="SemiBold"
                        BorderThickness="0" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
