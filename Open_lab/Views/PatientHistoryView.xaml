<UserControl x:Class="Open_lab.Views.PatientHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Lab ID" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox Text="{Binding LabId, UpdateSourceTrigger=PropertyChanged}" Width="120" />
            <TextBlock Text="من" VerticalAlignment="Center" Margin="12,0,8,0" />
            <DatePicker SelectedDate="{Binding From}" Width="140" />
            <TextBlock Text="إلى" VerticalAlignment="Center" Margin="12,0,8,0" />
            <DatePicker SelectedDate="{Binding To}" Width="140" />
            <Button Content="تحميل" Margin="12,0,0,0" Command="{Binding LoadHistoryCommand}" />
            <Button Content="طباعة التاريخ المرضي" Margin="8,0,0,0" Command="{Binding PrintHistoryCommand}" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,12,0,0">
            <TextBlock Text="{Binding StatusMessage}" Foreground="DarkGreen" TextWrapping="Wrap" />
            <ItemsControl ItemsSource="{Binding Visits}" Margin="0,8,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#DDD" BorderThickness="1" Margin="0,4,0,4" Padding="8">
                            <StackPanel>
                                <TextBlock Text="{Binding Visit.VisitDate}" FontWeight="Bold" />
                                <ItemsControl ItemsSource="{Binding Tests}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Test.NameReport}" Width="200" />
                                                <TextBlock Text="{Binding VisitTest.Status}" Width="120" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>
