<UserControl x:Class="Open_lab.Views.PriceListsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <StackPanel Margin="0,0,16,0">
            <TextBlock Text="قوائم الأسعار" FontSize="20" FontWeight="Bold" />
            <TextBlock Text="اسم القائمة" Margin="0,12,0,4" />
            <TextBox Text="{Binding ListName, UpdateSourceTrigger=PropertyChanged}" />
            <CheckBox Content="افتراضية" IsChecked="{Binding IsDefault}" Margin="0,8,0,0" />
            <Button Content="إنشاء قائمة" Margin="0,8,0,0" Command="{Binding SaveListCommand}" />

            <TextBlock Text="القائمة الحالية" Margin="0,12,0,4" />
            <ComboBox ItemsSource="{Binding PriceLists}" SelectedItem="{Binding SelectedPriceList}" DisplayMemberPath="Name" />

            <TextBlock Text="التحليل" Margin="0,12,0,4" />
            <ComboBox ItemsSource="{Binding Tests}" SelectedItem="{Binding SelectedTest}" DisplayMemberPath="NameReport" />
            <TextBlock Text="السعر" Margin="0,12,0,4" />
            <TextBox Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="إضافة" Margin="0,8,0,0" Command="{Binding AddItemCommand}" />
            <TextBlock Text="{Binding StatusMessage}" Foreground="DarkGreen" Margin="0,8,0,0" />
        </StackPanel>

        <DataGrid Grid.Column="1" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="التحليل" Binding="{Binding Test.NameReport}" Width="*" />
                <DataGridTextColumn Header="السعر" Binding="{Binding Price}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
