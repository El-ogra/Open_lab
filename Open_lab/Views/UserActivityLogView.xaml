<UserControl x:Class="Open_lab.Views.UserActivityLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <WrapPanel Margin="0,0,0,12">
            <TextBlock Text="عدد السجلات" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox Width="120" Text="{Binding MaxCount, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="تحميل السجل" Margin="12,0,0,0" Command="{Binding LoadCommand}" />
        </WrapPanel>

        <TextBlock Grid.Row="1" Text="{Binding StatusMessage}" Foreground="DarkGreen" Margin="0,0,0,8" />

        <DataGrid Grid.Row="2" ItemsSource="{Binding Items}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="التاريخ" Binding="{Binding Timestamp, StringFormat=yyyy-MM-dd HH:mm:ss}" Width="170" />
                <DataGridTextColumn Header="المستخدم" Binding="{Binding Username}" Width="120" />
                <DataGridTextColumn Header="الإجراء" Binding="{Binding Action}" Width="90" />
                <DataGridTextColumn Header="الجدول" Binding="{Binding TableName}" Width="120" />
                <DataGridTextColumn Header="وصف النشاط" Binding="{Binding ActivityDescription}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
