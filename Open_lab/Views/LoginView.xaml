<UserControl x:Class="Open_lab.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid Margin="40">
        <StackPanel>
            <TextBlock Text="تسجيل الدخول" FontSize="24" FontWeight="Bold" />
            <TextBlock Text="اسم المستخدم" Margin="0,16,0,4" />
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="كلمة المرور" Margin="0,12,0,4" />
            <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" />
            <CheckBox Content="تذكرني" IsChecked="{Binding RememberMe}" Margin="0,12,0,0" />
            <Button Content="دخول" Margin="0,16,0,0" Command="{Binding LoginCommand}" />
            <TextBlock Text="{Binding StatusMessage}" Foreground="Crimson" Margin="0,12,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
