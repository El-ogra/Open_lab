<UserControl x:Class="Open_lab.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:behaviors="clr-namespace:Open_lab.Views.Behaviors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             FlowDirection="RightToLeft"
             d:DesignHeight="550" d:DesignWidth="400">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        
        <Style x:Key="ModernInputField" TargetType="Control">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FlowDirection" Value="LeftToRight"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Control">
                        <Border x:Name="border" 
                                Background="White" 
                                BorderBrush="#D1D1D1" 
                                BorderThickness="1" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#106EBE"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#106EBE"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LoginButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#106EBE"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#004578"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#C8C8C8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="EyeButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Width" Value="35"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent">
                            <TextBlock x:Name="icon" 
                                       Text="&#xE723;" 
                                       FontFamily="Segoe MDL2 Assets" 
                                       FontSize="16" 
                                       Foreground="#605E5C" 
                                       HorizontalAlignment="Center" 
                                       VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="White">
        <Border Width="360" Height="Auto" 
                VerticalAlignment="Center" 
                HorizontalAlignment="Center"
                BorderBrush="#F3F5F7"
                BorderThickness="1"
                CornerRadius="8"
                Padding="30">
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Title -->
                    <RowDefinition Height="*"/>    <!-- Inputs -->
                    <RowDefinition Height="Auto"/> <!-- Message -->
                    <RowDefinition Height="Auto"/> <!-- Button -->
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,25">
                    <TextBlock Text="تسجيل الدخول" 
                               FontSize="26" 
                               FontWeight="ExtraBold" 
                               Foreground="#1B3B6F"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="Open Lab System" 
                               FontSize="12" 
                               Foreground="#106EBE"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Center"
                               Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1">
                    <TextBlock Text="اسم المستخدم" FontWeight="SemiBold" Foreground="#323130" Margin="0,0,0,8"/>
                    <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource ModernInputField}" />

                    <TextBlock Text="كلمة المرور" FontWeight="SemiBold" Foreground="#323130" Margin="0,20,0,8"/>
                    <Grid>
                        <!-- PasswordBox -->
                        <PasswordBox x:Name="PasswordInput" 
                                     behaviors:PasswordBoxAssistant.BindPassword="True"
                                     behaviors:PasswordBoxAssistant.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                            <PasswordBox.Style>
                                <Style TargetType="PasswordBox" BasedOn="{StaticResource ModernInputField}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsPasswordVisible}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </PasswordBox.Style>
                        </PasswordBox>
                        
                        <!-- TextBox (Visible when toggled) -->
                        <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}">
                            <TextBox.Style>
                                <Style TargetType="TextBox" BasedOn="{StaticResource ModernInputField}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsPasswordVisible}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>

                        <!-- Eye Toggle Button -->
                        <Button HorizontalAlignment="Left" 
                                VerticalAlignment="Center"
                                Command="{Binding TogglePasswordVisibilityCommand}"
                                Style="{StaticResource EyeButtonStyle}">
                            <Button.Resources>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsPasswordVisible}" Value="True">
                                            <Setter Property="Text" Value="&#xE7B3;"/>
                                            <Setter Property="Foreground" Value="#106EBE"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                        <TextBlock Text="تذكر بياناتي" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="#605E5C" FontSize="13"/>
                        <CheckBox IsChecked="{Binding RememberMe}" 
                                  VerticalAlignment="Center" 
                                  FlowDirection="LeftToRight"/>
                    </StackPanel>
                </StackPanel>

                <TextBlock Grid.Row="2" 
                           Text="{Binding StatusMessage}" 
                           Foreground="#D13438" 
                           FontSize="12"
                           TextWrapping="Wrap"
                           Margin="0,10"
                           TextAlignment="Center"
                           MinHeight="18"/>

                <Button Grid.Row="3" 
                        Content="دخول" 
                        IsDefault="True"
                        Command="{Binding LoginCommand}"
                        Style="{StaticResource LoginButtonStyle}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
