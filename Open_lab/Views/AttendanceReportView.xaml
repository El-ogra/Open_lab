<UserControl x:Class="Open_lab.Views.AttendanceReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <GroupBox Header="خيارات التقرير" Grid.Row="0" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal" Margin="8">
                <TextBlock Text="من:" VerticalAlignment="Center" Margin="0,0,8,0" />
                <DatePicker SelectedDate="{Binding From}" Width="140" />
                <TextBlock Text="إلى:" VerticalAlignment="Center" Margin="12,0,8,0" />
                <DatePicker SelectedDate="{Binding To}" Width="140" />
                <TextBlock Text="المستخدم:" VerticalAlignment="Center" Margin="12,0,8,0" />
                <ComboBox ItemsSource="{Binding Users}" 
                          SelectedValue="{Binding SelectedUserId}" 
                          SelectedValuePath="UserId"
                          DisplayMemberPath="Username" 
                          Width="180" Margin="0,0,12,0" />
                <Button Content="إنشاء التقرير" Command="{Binding GenerateReportCommand}" Margin="0,0,8,0" />
                <Button Content="مسح الفلتر" Command="{Binding ClearFilterCommand}" />
            </StackPanel>
        </GroupBox>

        <DataGrid Grid.Row="1" ItemsSource="{Binding ReportRows}" AutoGenerateColumns="False" Margin="0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="المستخدم" Binding="{Binding Username}" Width="120" />
                <DataGridTextColumn Header="تسجيل الدخول" Binding="{Binding LoginAt, StringFormat=yyyy-MM-dd HH:mm}" Width="150" />
                <DataGridTextColumn Header="تسجيل الخروج" Binding="{Binding LogoutAt, StringFormat=yyyy-MM-dd HH:mm}" Width="150" />
                <DataGridTextColumn Header="الوردية" Binding="{Binding ShiftName}" Width="100" />
                <DataGridTextColumn Header="إجمالي الساعات" Binding="{Binding TotalHours, StringFormat=F2}" Width="110" />
                <DataGridTextColumn Header="التأخير (دقيقة)" Binding="{Binding DelayMinutes, StringFormat=F0}" Width="110" />
                <DataGridTextColumn Header="العمل الإضافي (دقيقة)" Binding="{Binding OvertimeMinutes, StringFormat=F0}" Width="130" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Foreground="DarkGreen" TextWrapping="Wrap" />
    </Grid>
</UserControl>
