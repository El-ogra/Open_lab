<UserControl x:Class="Open_lab.Views.TestClassificationLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <WrapPanel Margin="0,0,0,12">
            <TextBlock Text="من" VerticalAlignment="Center" Margin="0,0,8,0" />
            <DatePicker SelectedDate="{Binding From}" Width="140" />
            <TextBlock Text="إلى" VerticalAlignment="Center" Margin="12,0,8,0" />
            <DatePicker SelectedDate="{Binding To}" Width="140" />
            <Button Content="تحميل" Margin="12,0,0,0" Command="{Binding LoadCommand}" />
            <Button Content="طباعة" Margin="8,0,0,0" Command="{Binding PrintCommand}" />
        </WrapPanel>

        <TextBlock Grid.Row="1" Text="{Binding StatusMessage}" Foreground="DarkGreen" Margin="0,0,0,8" />

        <DataGrid Grid.Row="2" ItemsSource="{Binding Items}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="اسم الكاشف" Binding="{Binding ReagentName}" Width="*" />
                <DataGridTextColumn Header="الاستهلاك" Binding="{Binding TotalConsumed, StringFormat=N2}" Width="140" />
                <DataGridTextColumn Header="الوحدة" Binding="{Binding Unit}" Width="100" />
                <DataGridTextColumn Header="عدد التحاليل" Binding="{Binding TestCount}" Width="140" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
