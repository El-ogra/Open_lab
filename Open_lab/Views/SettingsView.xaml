<UserControl x:Class="Open_lab.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             FlowDirection="RightToLeft">
    <Grid Margin="20">
        <TabControl>
            <TabItem Header="الطابعات">
                <StackPanel Margin="20">
                    <TextBlock Text="الطابعة الافتراضية:" Margin="0,5"/>
                    <ComboBox ItemsSource="{Binding AvailablePrinters}" SelectedItem="{Binding DefaultPrinter}"/>
                    <TextBlock Text="طابعة الإيصالات:" Margin="0,5"/>
                    <ComboBox ItemsSource="{Binding AvailablePrinters}" SelectedItem="{Binding ReceiptPrinter}"/>
                    <TextBlock Text="طابعة التقارير:" Margin="0,5"/>
                    <ComboBox ItemsSource="{Binding AvailablePrinters}" SelectedItem="{Binding ReportPrinter}"/>
                    <Button Content="حفظ إعدادات الطابعات" Command="{Binding SavePrinterSettingsCommand}" Margin="0,20"/>
                </StackPanel>
            </TabItem>
            <TabItem Header="الهوامش">
                <StackPanel Margin="20">
                    <TextBlock Text="الهامش الأيسر (سم):" Margin="0,5"/>
                    <TextBox Text="{Binding LeftMargin}"/>
                    <TextBlock Text="الهامش الأيمن (سم):" Margin="0,5"/>
                    <TextBox Text="{Binding RightMargin}"/>
                    <Button Content="حفظ إعدادات الهوامش" Command="{Binding SaveMarginSettingsCommand}" Margin="0,20"/>
                </StackPanel>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
