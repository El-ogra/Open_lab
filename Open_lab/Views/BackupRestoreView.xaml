<UserControl x:Class="Open_lab.Views.BackupRestoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <GroupBox Header="النسخ الاحتياطي">
            <StackPanel Margin="8">
                <TextBlock Text="مسار النسخة" Margin="0,0,0,4" />
                <TextBox Text="{Binding BackupPath, UpdateSourceTrigger=PropertyChanged}" />
                <Button Content="إنشاء نسخة" Margin="0,8,0,0" Command="{Binding BackupCommand}" />
            </StackPanel>
        </GroupBox>

        <GroupBox Header="الاستعادة" Grid.Row="1" Margin="0,12,0,0">
            <StackPanel Margin="8">
                <TextBlock Text="مسار الملف" Margin="0,0,0,4" />
                <TextBox Text="{Binding RestorePath, UpdateSourceTrigger=PropertyChanged}" />
                <Button Content="استعادة" Margin="0,8,0,0" Command="{Binding RestoreCommand}" />
            </StackPanel>
        </GroupBox>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Foreground="DarkGreen" Margin="0,12,0,0" />
    </Grid>
</UserControl>
