<UserControl x:Class="Open_lab.Views.WorkSheetByTestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <TextBlock Text="من" VerticalAlignment="Center" Margin="0,0,8,0" />
            <DatePicker SelectedDate="{Binding From}" Width="140" />
            <TextBlock Text="إلى" VerticalAlignment="Center" Margin="12,0,8,0" />
            <DatePicker SelectedDate="{Binding To}" Width="140" />
            <Button Content="تحميل" Margin="12,0,0,0" Command="{Binding LoadCommand}" />
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Rows}" AutoGenerateColumns="False" Margin="0,12,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="التحليل" Binding="{Binding TestName}" Width="*" />
                <DataGridTextColumn Header="العدد" Binding="{Binding Count}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
