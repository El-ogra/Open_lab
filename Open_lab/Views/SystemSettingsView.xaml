<UserControl x:Class="Open_lab.Views.SystemSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <TextBlock Text="المفتاح" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox Text="{Binding Key, UpdateSourceTrigger=PropertyChanged}" Width="200" />
            <TextBlock Text="القيمة" VerticalAlignment="Center" Margin="12,0,8,0" />
            <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Width="260" />
            <Button Content="حفظ" Margin="12,0,0,0" Command="{Binding SaveCommand}" />
            <Button Content="حذف" Margin="8,0,0,0" Command="{Binding DeleteCommand}" />
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Settings}" SelectedItem="{Binding SelectedSetting}" AutoGenerateColumns="False" Margin="0,12,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="المفتاح" Binding="{Binding Key}" Width="220" />
                <DataGridTextColumn Header="القيمة" Binding="{Binding Value}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Foreground="DarkGreen" Margin="0,12,0,0" />
    </Grid>
</UserControl>
