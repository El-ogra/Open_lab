<UserControl x:Class="Open_lab.Views.ExternalLabManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TabControl Grid.Row="1">
            <TabItem Header="قائمة الانتظار">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Button Content="تحديث القائمة" Command="{Binding LoadQueueCommand}" Margin="0,0,8,0" />
                        <ComboBox ItemsSource="{Binding Referrals}" 
                                  SelectedValue="{Binding SelectedReferralId}" 
                                  SelectedValuePath="ReferralId"
                                  DisplayMemberPath="Name" 
                                  Width="200" Margin="0,0,8,0" />
                        <Button Content="إنشاء بوليصة شحن" Command="{Binding CreateManifestCommand}" />
                    </StackPanel>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding PendingQueue}" AutoGenerateColumns="False" 
                              SelectedItem="{Binding SelectedQueueItem}" SelectionMode="Extended">
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Header="اختيار" Binding="{Binding IsSelected}" Width="60" />
                            <DataGridTextColumn Header="المريض" Binding="{Binding PatientName}" Width="150" />
                            <DataGridTextColumn Header="التحليل" Binding="{Binding TestName}" Width="200" />
                            <DataGridTextColumn Header="الحالة" Binding="{Binding Status}" Width="100" />
                            <DataGridTextColumn Header="تاريخ الإضافة" Binding="{Binding DateQueued, StringFormat=yyyy-MM-dd HH:mm}" Width="150" />
                            <DataGridTextColumn Header="المرجع الخارجي" Binding="{Binding ExternalReference}" Width="150" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <Button Grid.Row="2" Content="تحديث الحالة" Command="{Binding UpdateStatusCommand}" Margin="0,8,0,0" />
                </Grid>
            </TabItem>

            <TabItem Header="بوليصات الشحن">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Button Content="تحديث" Command="{Binding LoadManifestsCommand}" Margin="0,0,0,8" HorizontalAlignment="Left" />

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Manifests}" AutoGenerateColumns="False" SelectedItem="{Binding SelectedManifest}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="رقم البوليصة" Binding="{Binding ManifestNumber}" Width="150" />
                            <DataGridTextColumn Header="التاريخ" Binding="{Binding DateCreated, StringFormat=yyyy-MM-dd HH:mm}" Width="150" />
                            <DataGridTextColumn Header="الحالة" Binding="{Binding Status}" Width="100" />
                            <DataGridTextColumn Header="ملاحظات" Binding="{Binding CourierNotes}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="التسويات المالية">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="المعمل:" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <ComboBox ItemsSource="{Binding Referrals}" 
                                  SelectedValue="{Binding SelectedReferralId}" 
                                  SelectedValuePath="ReferralId"
                                  DisplayMemberPath="Name" 
                                  Width="200" Margin="0,0,8,0" />
                        <Button Content="عرض الرصيد والتسويات" Command="{Binding LoadSettlementCommand}" />
                    </StackPanel>

                    <GroupBox Header="إنشاء تسوية جديدة" Grid.Row="1" Margin="0,0,0,8">
                        <StackPanel Orientation="Horizontal" Margin="8">
                            <TextBlock Text="المبلغ:" VerticalAlignment="Center" Margin="0,0,8,0" />
                            <TextBox Text="{Binding SettlementAmount}" Width="100" Margin="0,0,12,0" />
                            <TextBlock Text="ملاحظات:" VerticalAlignment="Center" Margin="0,0,8,0" />
                            <TextBox Text="{Binding SettlementNote}" Width="200" Margin="0,0,12,0" />
                            <Button Content="تسجيل التسوية" Command="{Binding CreateSettlementCommand}" />
                        </StackPanel>
                    </GroupBox>

                    <DataGrid Grid.Row="2" ItemsSource="{Binding SettlementHistory}" AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="التاريخ" Binding="{Binding SettlementDate, StringFormat=yyyy-MM-dd HH:mm}" Width="150" />
                            <DataGridTextColumn Header="المبلغ المدفوع" Binding="{Binding AmountPaid, StringFormat=N2}" Width="120" />
                            <DataGridTextColumn Header="إجمالي التكلفة" Binding="{Binding TotalCost, StringFormat=N2}" Width="120" />
                            <DataGridTextColumn Header="الرصيد" Binding="{Binding Balance, StringFormat=N2}" Width="120" />
                            <DataGridTextColumn Header="ملاحظات" Binding="{Binding Note}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Foreground="DarkGreen" TextWrapping="Wrap" Margin="0,8,0,0" />
    </Grid>
</UserControl>
