<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Colors based on Reference Image 2 -->
    <SolidColorBrush x:Key="DeliveryMainGreenBg" Color="#2F4F2F" /> <!-- Dark Olive Green -->
    <SolidColorBrush x:Key="DeliveryPanelGreen" Color="#557C55" />
    <SolidColorBrush x:Key="DeliveryHeaderBlue" Color="#306E9A" />
    <SolidColorBrush x:Key="DeliveryOrange" Color="#E58D2D" />
    <SolidColorBrush x:Key="DeliveryYellow" Color="#FFCC00" />
    <SolidColorBrush x:Key="DeliveryLightYellowBg" Color="#FDFBD4" />
    <SolidColorBrush x:Key="DeliveryGreyWhite" Color="#F4F4F4" />

    <!-- DataGrid Header -->
    <Style x:Key="DeliveryDataGridHeaderStyle" TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="White" />
        <Setter Property="Foreground" Value="Black" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="BorderBrush" Value="#888" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="Padding" Value="5" />
    </Style>

    <!-- Info Box Headers (Blue/Orange) -->
    <Style x:Key="InfoBoxHeaderStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource DeliveryHeaderBlue}" />
        <Setter Property="BorderBrush" Value="White" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="4,2" />
        <Setter Property="Margin" Value="2" />
    </Style>

    <Style x:Key="InfoBoxValueStyle" TargetType="Border">
        <Setter Property="Background" Value="White" />
        <Setter Property="BorderBrush" Value="Black" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="4,2" />
        <Setter Property="Margin" Value="2" />
    </Style>

    <!-- Buttons -->
    <Style x:Key="DeliveryBaseButtonStyle" TargetType="Button">
        <Setter Property="Margin" Value="2" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="BorderBrush" Value="#444" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style x:Key="DeliveryYellowButtonStyle" TargetType="Button" BasedOn="{StaticResource DeliveryBaseButtonStyle}">
        <Setter Property="Background" Value="{StaticResource DeliveryYellow}" />
        <Setter Property="Foreground" Value="Black" />
    </Style>

    <Style x:Key="DeliveryGreenButtonStyle" TargetType="Button" BasedOn="{StaticResource DeliveryBaseButtonStyle}">
        <Setter Property="Background" Value="#A0D030" />
        <Setter Property="Foreground" Value="Black" />
    </Style>

    <Style x:Key="DeliveryDarkGreenButtonStyle" TargetType="Button" BasedOn="{StaticResource DeliveryBaseButtonStyle}">
        <Setter Property="Background" Value="#006400" />
        <Setter Property="Foreground" Value="White" />
    </Style>
    
    <Style x:Key="DeliveryBlueButtonStyle" TargetType="Button" BasedOn="{StaticResource DeliveryBaseButtonStyle}">
        <Setter Property="Background" Value="#306E9A" />
        <Setter Property="Foreground" Value="White" />
    </Style>
</ResourceDictionary>
